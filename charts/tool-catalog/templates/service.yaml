apiVersion: v1
kind: Service
metadata:
  name: tool-catalog
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: tool-catalog
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
